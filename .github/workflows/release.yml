name: NPM Publish

on:
    release:
        types: [published]

jobs:
    publish:
        runs-on: ubuntu-latest

        steps:
            - name: Set up Node.js
              uses: actions/setup-node@v1
              with:
                  node-version: "14.x"

            - name: Publish package
              run: npm publish
